#include <stdio.h>
#include "main.h"

/**
 * add - function that adds two integers
 * @a: first integer
 * @b: second integer
 * Return: the result of the addition
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
