#include "main.h"

/**
 * _strchr - locate first occurrence of char in string and retuens pointer
 * there
 * @s: string to search
 * @c: target character
 * Return: pointer to that character in string
 */
char *_strchr(char *s, char c)
{
	return (s);
}
